package paymentprovider

// debo pensar esta cosa porque segun, esta madre recibe
// credenciales de usuario tipo stripe, no? o ese era paypal?
func Pay(uuid, tableid string) (bool, error) {
	return true, nil
}
